import AdminComingSoon from "@/components/AdminComingSoon"
import { Receipt } from "lucide-react"

export default function Page() {
  return <AdminComingSoon title="Subscriptions" subtitle="Track plan subscriptions and renewals across tenants." icon={Receipt} />
}
